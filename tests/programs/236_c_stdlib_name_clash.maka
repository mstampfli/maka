int div(int a, int b) { if (b == 0) { return 0; } return a / b; }
int abs(int x) { if (x < 0) { return 0 - x; } return x; }
unit main() {
    log(div(20, 4));   // 5
    log(abs(-9));      // 9
    log(div(7, 0));    // 0
}
