// Spawn-tier closures (thread/job/spawn_pool) reject borrowed captures.
// The borrow's lifetime is tied to the caller's scope but the thread can
// outlive it — would race or dangle.
unit main() {
    mut int counter = 0;
    *Thread t = thread(unit() [&mut counter] { counter = counter + 1; });
    join(t);
}
