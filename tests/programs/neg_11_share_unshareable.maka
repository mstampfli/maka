// Sharing a mutable Vec (non-Shareable) should be rejected.
unit consume(*int p) { log(p!); }

unit main() {
    *int p = alloc 5;
    consume(share p);   // *T (mutable ptr) is NOT Shareable
    free(p);
}
