// Mutex from the stdlib (typed handle wrapping the pthread-backed runtime).
import std.{Mutex, mutex_new, mutex_lock, mutex_unlock, mutex_destroy};

unit main() {
    Mutex mu = mutex_new();
    mutex_lock(mu);
    log("locked");
    mutex_unlock(mu);
    log("unlocked");
    mutex_destroy(mu);
}
