// §10.2 — Generic functions, type inferred from args.
int twice<T>(T x) {
    return 7 + 7;
}

logic Box {
    int identity<T>(T x) {
        return 42;
    }
}

unit main() {
    log(twice(1));              // 14, T inferred = int
    log(Box.identity(99));      // 42
}
