warning at 13:9: pointer `local` was auto-nulled when its pointee went out of scope and has not been explicitly re-assigned on every code path since; this use observes that silent overwrite - re-assign `local` yourself before reading it
warning at 13:30: pointer `local` was auto-nulled when its pointee went out of scope and has not been explicitly re-assigned on every code path since; this use observes that silent overwrite - re-assign `local` yourself before reading it
-1
