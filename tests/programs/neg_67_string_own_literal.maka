// A borrowed string is non-owning; making it an owned `String` requires
// allocating (e.g. concat or a builtin), so an implicit literal->owned that
// would alias and later free a static literal must be rejected.
unit main() {
  String s = "x";
  log(s);
}
