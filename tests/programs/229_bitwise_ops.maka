unit main() {
    int a = 12;     // 1100
    int b = 10;     // 1010
    log(a & b);     // 8  (1000)
    log(a | b);     // 14 (1110)
    log(a ^ b);     // 6  (0110)
    log(1 << 4);    // 16
    log(256 >> 3);  // 32
}
