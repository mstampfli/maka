// `pub attr` + `pub has` exports the trait and its impl across modules, so a
// consumer's `use shapes.Color.Drawable;` brings the impl into bound-check scope.
module shapes;

pub data Color { int rgb; }

pub attr Drawable { unit draw(&_ self); }
pub Color has Drawable {
    unit draw(&Color self) { log(self.rgb); }
}
