[workspace]
resolver = "2"
members = [
    "crates/lexer",
    "crates/ast",
    "crates/parser",
    "crates/sema",
    "crates/codegen",
    "crates/driver",
]
# A fixture crate for the rdep link-metadata test (427); a path dependency of a
# generated rblock sidecar, never part of this workspace.
exclude = [
    "tests/programs/427_rdep_linkmeta_crate",
]

[workspace.package]
edition = "2021"
version = "0.1.0"
license = "MIT"

[workspace.dependencies]
maka_lexer = { path = "crates/lexer" }
maka_ast    = { path = "crates/ast" }
maka_parser = { path = "crates/parser" }
maka_sema   = { path = "crates/sema" }
maka_codegen = { path = "crates/codegen" }

[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = "thin"
